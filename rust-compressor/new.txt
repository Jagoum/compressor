/ main.rs
use axum:{
 routing:get,
 Router,
 Json,
};
use serde:{Deserialize, Serialize};
use std:net:SocketAdr;
use tokio;
use tracing_subscriber;
use tower_htp:cors:{CorsLayer, Any};
#[derive(Serialize, Deserialize)]
struct ApiResponse {
 mesage: String,
}
async fn handler() -> Json<ApiResponse> {
 Json(ApiResponse {
 mesage: "Helo, world!".to_string(),
 })
}
#[tokio:main]
async fn main() {
 / Initialize tracing subscriber
 tracing_subscriber:fmt:init();
 / Build our aplication with a route
 let ap = Router:new()
 .route("/", get(handler)
 .layer(CorsLayer:new().alow_origin(Any);
 / Run our aplication
 let adr = SocketAdr:from([127, 0, 0, 1], 30);
 tracing:debug!("listening on {}", adr);
 axum:Server:bind(&adr)
 .serve(ap.into_make_service()
 .await
 .unwrap();
}
/ Cargo.toml
[package]
name = "rust_api"
version = "0.1.0"
edition = "2021"
[dependencies]
axum = {version = "0.5.13", features = ["headers"]}
serde = {version = "1.0", features=["derive"]}
serde_json= "1.0.68"
tokio= {version = "1.0", features = ["ful"]}
tracing= "0.1"
tracing-subscriber = {version = "0.3.0", features = ["env-filter"]}
tower-htp = {version = "0.3.0", features = ["cors"]}
